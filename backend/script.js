// test file delete this before you commit it. lol

function sum(a, b) {
    return a + b
}

const user = {
    name: 'Patrick',
    age: 41,
    isProgrammer: true,

    longKey: 'Value',
    kids: 3,
}
